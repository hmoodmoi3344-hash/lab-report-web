# 项目总览 · 工科实验报告样本库（网站版）

> 本文件由 WorkBuddy 从仓库的 git 提交历史与源码结构还原整理。
> 源工作区：`实验报告网站`（2026-08-10 10:58 创建）→ 已整体迁入本工作区 `实验网站`。
>
> ⚠️ **重要部署信息（2026-08-11 补充）：**
> 本站**不是 GitHub Pages 部署**，而是已部署到**真实 VPS 服务器**上：
> - 服务器 IP：`62.234.211.80`
> - 面板：宝塔面板（BT Panel），Nginx 1.30.4
> - 站点根目录：`/www/wwwroot/exp-sample.tqp`
> - 状态：**运行中**（2026-08-10 10:05 上传完毕）
> - 备案进度：**✅ ICP 备案已通过（2026-08-21）** → 域名 `exp-sample.cn`（.cn，DNSPod，2027-08-12 到期）→ **✅ DNS 已解析到 VPS `62.234.211.80`** → **✅ 宝塔已绑域名 + 申请 Let's Encrypt SSL + 强制 HTTPS** → **站点已上线：https://exp-sample.cn**（2026-08-21）→ 下一步：接广告（百度联盟/AdSense）
>
> 说明：由于无法检索到当天对话原文，下面"从头到尾做了什么"依据仓库 4 次提交记录还原，可能与你记忆中的步骤一致。

---

## 一、这是个什么项目

把**小程序里的工科实验报告样例**，做成一个**可被百度/Google 收录的独立静态内容站**。核心增量是「**数据处理计算器**」：学生把自己的实测数据填进去，页面实时算出处理结果（应力、效率、传热系数、雷诺数……）。

- 内容量：覆盖 26 个工科专业、**100+ 实验**。
- 形态：纯静态站（HTML + JS + 数据 JS），可挂 GitHub Pages / 任意静态托管。
- 定位声明（每页已明示）：全部数据均为 **AI 虚构仿写演示样例**，仅供报告**格式与结构参考**，严禁直接复制提交作为课程作业。

---

## 二、迁入后本工作区的结构

```
实验网站/
  index.html            首页（按课程分组的课程目录 + 搜索 + 投稿入口）
  exp.html              实验详情页（动态，?id=xxx）
  submit.html           实验投稿页（UGC，学生补充自己的实验数据）
  about.html / contact.html / privacy.html / disclaimer.html   四法定页（广告准入用）
  css/style.css         响应式样式 + 广告位占位 + 投稿表单样式
  js/
    app.js              首页逻辑（搜索/课程分组渲染）
    exp-detail.js       详情页渲染 + 计算器挂载
    calculators.js      真实公式计算器（覆盖 55 个实验）
    submit.js           投稿表单逻辑（本地草稿 + 邮件/Formspree 提交）
  data/site-data.js     由 build.py 生成的全局数据（window.SITE_DATA）
  exp/<id>.html         每个实验一个独立静态 SEO 页（共 100 个）
  src/data/*.json       源数据副本（majors / courses / experiments / hot_tags）
  build.py             统一构建脚本（生成 site-data.js + exp/*.html + sitemap + robots）
  build_pages.py       旧版生成脚本（已被 build.py 取代，可保留）
  converter.py         小程序 JSON → site-data.js 的转换脚本
  sitemap.xml / robots.txt
  .github/workflows/deploy.yml   GitHub Pages 自动构建部署
  .git/                 完整提交历史（见第三节）
```

---

## 三、从头到尾做了什么（git 历史还原）

| # | 提交 | 做了什么 |
|---|------|----------|
| 1 | `9c7d224` Initial | **搭起整个静态站骨架**：100 实验样本 + 数据处理计算器 + SEO 静态页。包含首页/详情页/投稿页、响应式 CSS（含广告位占位）、4 个 JS、全局数据、每个实验一个静态 SEO 页、converter/build_pages 脚本、sitemap/robots、以及 GitHub Pages 部署 workflow。此时真实公式覆盖 **11** 个实验。 |
| 2 | `651d2df` feat | **新增 44 个真实公式计算器**，覆盖从 11 提升到 **55** 个实验（材料/电路电子/化工/环监食品/机械热力/车辆地质采矿等方向）。其余实验详情页显示"计算器持续补充中"。 |
| 3 | `368872c` feat | **补齐广告准入四法定页**：新增 关于/联系/隐私政策/免责声明 四个页面 + 全站页脚链接 + 纳入 sitemap 收录。为接入**百度联盟 / AdSense** 做准备。 |
| 4 | `9db3ab3` fix | **替换联系邮箱占位符**为 `13609332612@163.com`（百度联盟/备案联系信息）。 |

一句话总结：**先搭站 → 补公式 → 补合规页 → 填联系信息**，四步把"能看的内容站"推进到"能申请广告变现"的状态。

---

## 四、当前状态（可用 / 已验证）

- ✅ 数据已整体迁入本工作区，100 个实验静态页 + 全局数据 + sitemap 都在。
- ✅ 已补齐 `src/data/*.json` 源数据，**站点现在可独立重新生成**（刚跑过 `build.py`，成功生成 107 个页面 + 数据 + sitemap）。
- ✅ git 历史完整迁入（4 次提交）。
- ✅ 四法定页 + 广告位占位 + 部署 workflow 已就绪。
- ⚠️ 计算器仅覆盖 55/100+ 实验，其余显示"持续补充中"。
- ⚠️ 广告位还是**空占位**，未接入真实广告代码；投稿页还是**本地草稿 + 邮件/Formspree**，无自动收录后端。

---

## 五、接下来该做什么（按优先级）

### P0 — 变现接入（广告）
1. 申请 **百度联盟**（需已备案域名 + 四法定页，本站已备齐）和/或 **Google AdSense**。
2. 拿到广告代码后，替换各页 `<div class="ad-slot">` 占位（首页顶部/列表底部/详情页底部）。
3. **域名与 HTTPS 已全部完成**：DNS A 记录指向 VPS `62.234.211.80`，宝塔已绑 `exp-sample.cn` + `www.exp-sample.cn`，Let's Encrypt SSL 已部署并强制 HTTPS，站点现已上线 **https://exp-sample.cn**。`build.py` 的 `SITE_URL` 已改为 `https://exp-sample.cn`（sitemap/robots 已重build 指向新域名）。百度联盟对备案 `.cn` 域名最友好，可直接申请。

### P1 — 计算器覆盖率
4. 按 `js/calculators.js` 现有格式，继续补剩余专业的真实公式（机械部分、建筑环境、航海/农业/食品/地质/采矿等），把"持续补充中"逐步变成可用计算器。

### P1 — UGC 投稿闭环
5. `submit.html` 现在只能存浏览器本地草稿 + 邮件/Formspree 发给你。**接 CloudBase / Formspree 后端**可实现自动收录**：投稿 → 审核 → 写入 `src/data/experiments.json` → 重跑 `build.py` → 自动上线。

### P2 — SEO 收录推进
6. 在 **百度站长平台 / Google Search Console** 提交 `sitemap.xml`，并开启新页面主动推送。
7. 可给详情页补充 `schema.org` 结构化数据，提升搜索摘要质量。

### P2 — 数据与部署维护
8. **数据源真相**：本站 `src/data/` 是从小程序 `lab-report-miniprogram/data/` 复制的**副本**。改数据请以小程序为准（或两端同步），然后跑 `build.py` 重新生成。
9. 确认 GitHub 仓库 remote 指向 `lab-report-web` 且已开启 Pages；之后 `push main` 会自动构建并回写生成文件。

---

## 六、怎么跑 / 怎么重新生成

本地预览：
```bash
cd 实验网站
python -m http.server 8000
# 浏览器打开 http://localhost:8000/index.html
```

重新生成（数据有变动后）：
```bash
python build.py          # 读 src/data/*.json → 刷新 data/site-data.js + exp/*.html + sitemap + robots
```

---

## 七、重要提醒

- **数据虚构**：全部实验数据均为 AI 虚构仿写演示样例，仅供格式参考，严禁直接提交作为课程作业。若要产品化，需替换为真实可核验数据，或明确自身定位为"格式范例"。
- **原工作区未删除**：`实验报告网站`、`实验报告`（含小程序）仍保留在原处。确认无误后如需删除可另行告知（我不会擅自删）。
